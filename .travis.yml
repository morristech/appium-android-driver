language: android
dist: precise
before_script:
  # node stuff
  - curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.33.1/install.sh | bash
  - nvm install 8
  - node --version
  - npm --version

  - npm install opencv4nodejs
script:
  - npm ls opencv4nodejs
